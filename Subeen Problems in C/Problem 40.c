#include <stdio.h>
#include <math.h>
int main()
{
    int t,x,k,j,i,sum,p,l;
    scanf("%d", &t);
    for(i=0;i<t;i++)
    {
        scanf("%d%d", &x, &k);
        sum = 0;
        p = 1;
        for(j=0;j<=k;j++)
        {
            sum = sum + p;
            for(l=0;l<=1;l++)
            {

            }
        }
        printf("Result = %d\n", sum);
    }
    return 0;
}
