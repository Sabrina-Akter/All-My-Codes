#include <bits/stdc++.h>
using namespace std;

#define fastt ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define ll long long int

int main()
{
    fastt;
    string a, b;
    cin >> a >> b;
    cout << a.size() << " " << b.size() << "\n" << a << " " << b << endl;
    return 0;
}
