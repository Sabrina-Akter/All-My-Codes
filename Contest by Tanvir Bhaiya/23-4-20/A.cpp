#include <bits/stdc++.h>
using namespace std;

int main()
{
    long long t, sum=0, x;
    cin >> t;
    while(t--)
    {
        cin >> x;
        sum = sum + x;
    }
    cout << sum << endl;
    return 0;
}

