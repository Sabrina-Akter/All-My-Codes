#include <bits/stdc++.h>
using namespace std;

#define fastt ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
#define ll long long int
#define vll vector <ll>
#define pb push_back



int main()
{
    fastt;
    ll t;
    cin >> t;
    while(t--)
    {
        ll a, b, n, i, x, y;
        cin >> a >> b;
        ll A[a+1][b+1];
        cin >> n;
        for(i=0;i<n;i++)
        {
            cin >> x >> y;
        }
    }
    return 0;
}
